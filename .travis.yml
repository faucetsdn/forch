dist: bionic
sudo: required
language: python
python:
  - "3.7"
cach:
  pip: true
addons:
  apt:
    update: true
install:
  - bin/setup_base
script:
  - set -e
  - bin/check_style
  - bin/docker_build
  - bin/setup_stack local
  - sudo bin/test_stack
