/*
 * Encapsulation of device for DVA and role-based ACL assignment
 */
syntax = "proto3";

import "forch/proto/network_state.proto";

message Device {
    DeviceLearning learning = 1;
    DeviceBehavior behavior = 2;
}
